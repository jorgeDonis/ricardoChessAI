#include "chess_logic.h"
#include "stdlib.h"

game_state** expand_state(game_state* origin)
{
    game_state** node_list = (game_state**) malloc(sizeof(game_state) * MAX_NODES_MEM);
    
    return node_list;
}